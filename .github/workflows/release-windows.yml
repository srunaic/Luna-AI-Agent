name: Release (Windows) [DISABLED]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is intentionally disabled. Use .github/workflows/release-win.yml instead."
