{
  "name": "luna-ai-agent",
  "displayName": "Luna AI Agent",
  "description": "Luna AI Agent - High-performance AI Orchestrator",
  "version": "0.1.26",
  "repository": {
    "type": "git",
    "url": "https://github.com/srunaic/Luna-AI-Agent.git"
  },
  "main": "electron/main.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "build-webview": "cd webview-ui && npm run build",
    "dev-webview": "cd webview-ui && npm run dev",
    "electron": "electron .",
    "electron-dev": "ELECTRON_IS_DEV=true npm run electron",
    "build-electron": "npm run compile && npm run build-webview && electron-builder",
    "build-electron-win": "npm run compile && npm run build-webview && electron-builder --win --publish never",
    "release-win": "npm run compile && npm run build-webview && electron-builder --win --publish always",
    "release-win-auto": "node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json')); const d=new Date(); const ts=String(d.getFullYear()).slice(2)+('0'+(d.getMonth()+1)).slice(-2)+('0'+d.getDate()).slice(-2)+('0'+d.getHours()).slice(-2)+('0'+d.getMinutes()).slice(-2); p.version=p.version.split('-')[0]+'-v'+ts; fs.writeFileSync('package.json', JSON.stringify(p, null, 2))" && npm run release-win"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "electron-log": "^5.4.2",
    "electron-updater": "^6.6.2"
  },
  "build": {
    "appId": "com.lunaai.agent",
    "productName": "Luna AI Agent",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "electron/**/*",
      "out/**/*",
      "webview-ui/dist/**/*",
      "package.json",
      "node_modules/**/*",
      "assets/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/Luna.jpg",
      "requestedExecutionLevel": "requireAdministrator"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "srunaic",
        "repo": "Luna-AI-Agent",
        "releaseType": "release"
      }
    ]
  }
}